#pragma once
// =============================================================================
// config/credentials.h.example — TEMPLATE KREDENSIAL
// =============================================================================
// CARA PAKAI:
//   1. Salin file ini: cp credentials.h.example credentials.h
//   2. Edit credentials.h dengan nilai asli Anda
//   3. Jangan commit credentials.h ke git (sudah di .gitignore)
//
// File .example ini aman untuk di-commit — tidak berisi password asli.
// =============================================================================

namespace Wifi
{
    constexpr char SSID[]     = "NAMA_WIFI_ANDA";
    constexpr char PASSWORD[] = "PASSWORD_WIFI_ANDA";
}

namespace Mqtt
{
    // Cek IP broker dengan: ipconfig (Windows) / ip addr (Linux/Mac)
    constexpr char BROKER[]    = "192.168.X.X";
    constexpr uint16_t PORT    = 1883;
    constexpr char CLIENT_ID[] = "esp32_gateway";
    constexpr char USER[]      = "";   // kosongkan jika tidak pakai autentikasi
    constexpr char PASSWORD[]  = "";
}
